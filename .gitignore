node_modules/
.next/
out/
data/
.env
.env*.local
*.tsbuildinfo
next-env.d.ts
test-results/
playwright-report/
e2e/__screenshots__/
e2e/.auth/
.local/

# Android (Capacitor) — deploy/ANDROID.md. Gradle build outputs live under android/ and
# are already covered by android/.gitignore; these root-level patterns are belt-and-braces
# so a signing keystore can never be committed from any working directory.
*.jks
*.keystore
